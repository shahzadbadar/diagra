# Icons

Use Mermaid class syntax to attach icons:

```mermaid
A[Lambda]:::aws-lambda
B[Database]:::generic-database
```

Supported prefixes are `aws-`, `gcp-`, `azure-`, and `generic-`. Manifests live in `packages/icons`.
